$(document).ready(function(){
        $("body").css("background-color", "#FFFFED");
        $("*").css("background-color", "#FFFFED");
        $("*").css("color", "black");
        // var elements = $("*");
        // for (var i = 0; i < elements.length; i++) {
        //         var element = elements[i];
        //         var z_index = $(element).css("z-index");
        //         if (IsNumber(z_index)){
        //                 $(element).css("background-color", "#FFFFED");
        //         }
        // };
});